# NanoClaw Environment Configuration
# Copy to .env and customize

# Claude API token (required)
CLAUDE_CODE_OAUTH_TOKEN=your-token-here

# OpenAI API key (required for voice messages — Whisper STT + TTS)
# OPENAI_API_KEY=sk-...

# Stable dev token — never expires, bypasses temp token flow
# Generate with: openssl rand -hex 32
# DEV_TOKEN=your-stable-token-here

# Override port from channels.yaml (pour worktrees/instances parallèles)
# WEB_PORT=17284

# Port Vite dev server (chaque worktree doit avoir un port distinct)
# VITE_PORT=5174

# Désactiver Tailscale Funnel (localhost uniquement)
# TAILSCALE_FUNNEL=false

# Port HTTPS Tailscale Funnel (443 par défaut, 8443 ou 10000 pour coexister)
# FUNNEL_PORT=8443

# Agent models — un par agent, override individuellement
# MODEL_MAIN=claude-sonnet-4-6        # Agent principal (container)
# MODEL_RAG=claude-sonnet-4-6         # RAG agent (recherche mémoire)
# MODEL_CONTEXT=claude-sonnet-4-6     # Context agent (mise à jour mémoire)
# MODEL_TITLE=claude-haiku-4-5        # Title agent (titrage conversations)
